services:
  - type: web
    name: tenkasi-petitions
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: OFFICER_PIN
        value: thfvcbdkiem3640